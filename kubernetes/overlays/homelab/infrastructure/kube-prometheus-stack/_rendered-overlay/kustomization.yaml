# Stage-3 input. NS `monitoring` is in namespaces-psa.yaml AND vendor ships
# platform.io/provide.{logging-ship,monitoring-scrape}. Include vendor ns;
# dual ownership with namespaces-psa.yaml + alloy + loki overlays accepted.
# kustomize de-dupes by (apiVersion, kind, name) inside this build.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../../../../vendor/base/kubernetes/base/infrastructure/kube-prometheus-stack/namespace.yaml
  - ../../../../../../vendor/base/kubernetes/base/infrastructure/kube-prometheus-stack/_rendered/manifests.yaml
  - ../../../../../../vendor/base/kubernetes/base/infrastructure/kube-prometheus-stack/_rendered/crds.yaml
