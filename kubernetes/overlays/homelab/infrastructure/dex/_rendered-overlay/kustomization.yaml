# Stage-3 input. NS `dex` NOT in namespaces-psa.yaml — component owns ns.
# Vendor namespace.yaml carries platform.io/consume.* labels required for
# capability-consumer trust anchoring.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../../../../vendor/base/kubernetes/base/infrastructure/dex/namespace.yaml
  - ../../../../../../vendor/base/kubernetes/base/infrastructure/dex/_rendered/manifests.yaml
