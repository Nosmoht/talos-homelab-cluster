apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

generators:
  - ksops-generator.yaml

resources:
  - httproute.yaml
  - postgresql-cluster.yaml
  - serviceaccount.yaml
  - cnp-dex.yaml
  - cnp-postgresql.yaml
  - secret-store.yaml  # Phase A.2 — ESO SecretStore for kv/clusters/homelab/dex/*
