apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - cnp-kyverno-admission-controller.yaml
  - cnp-kyverno-background-controller.yaml
  - cnp-kyverno-cleanup-controller.yaml
  - cnp-kyverno-reports-controller.yaml
  - clusterpolicy-pi-reserved-daemonset-toleration.yaml
  # Phase C.3 — PreSync hook gating ClusterPolicy CRs on Kyverno
  # admission-controller webhook readiness.
  - presync-hook-sa.yaml
  - presync-hook-role.yaml
  - presync-hook-job.yaml
