# Stage-3 input. loki is a secondary tenant of the monitoring namespace.
# Namespace ownership belongs to kube-prometheus-stack (sole shipper of
# vendor monitoring/namespace.yaml in base v0.4.0+). loki deploys INTO
# the existing namespace without claiming ownership.
# See vendor/base/docs/adr-namespace-ownership-rendered-manifests.md.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../../../../vendor/base/kubernetes/base/infrastructure/loki/_rendered/manifests.yaml
