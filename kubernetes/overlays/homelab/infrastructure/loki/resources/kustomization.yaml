apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

generators:
  - ksops-generator.yaml

resources:
  - cnp-loki.yaml
  - external-secret-loki-minio-credentials.yaml  # Phase A.3 (uses SecretStore from kube-prometheus-stack overlay)
