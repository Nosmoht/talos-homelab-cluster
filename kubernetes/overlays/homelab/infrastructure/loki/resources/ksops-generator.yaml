apiVersion: viaduct.ai/v1
kind: ksops
metadata:
  name: loki-minio-credentials
  annotations:
    config.kubernetes.io/function: |
      exec:
        path: ksops
files:
  - secret-minio-credentials.sops.yaml
