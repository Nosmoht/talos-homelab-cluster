apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - rbac-vault-backend-ca.yaml
  - job-vault-config-bootstrap.yaml
  - backendtlspolicy-vault-ui.yaml
  - httproute-vault.yaml
  - policy-cert-manager-pki.yaml
  - kubernetes-auth-role-cert-manager-pki.yaml
  - secret-engine-mount-pki-root.yaml
  - pki-secret-engine-config-pki-root.yaml
  - secret-engine-mount-pki-int.yaml
  - pki-secret-engine-config-pki-int.yaml
  - pki-secret-engine-role-cert-manager-internal.yaml
  # Phase A.2: per-namespace Vault Policies + KubernetesAuthEngineRoles
  # for ESO. These reference VCO CRDs that come live with Phase B's
  # vault-config-operator deployment (sync-wave -3 in cutover). Until
  # then, ArgoCD reports them as "OutOfSync" — that is expected and
  # documented in plan-v2.md Phase A.
  - eso-policy-cert-manager.yaml
  - eso-policy-dex.yaml
  - eso-policy-monitoring.yaml
  - eso-policy-minio.yaml
  - eso-role-cert-manager.yaml
  - eso-role-dex.yaml
  - eso-role-monitoring.yaml
  - eso-role-minio.yaml
