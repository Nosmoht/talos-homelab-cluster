apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../../../base/infrastructure/multus-cni

labels:
  - pairs:
      app.kubernetes.io/instance: homelab
      app.kubernetes.io/part-of: homelab
    includeSelectors: false
