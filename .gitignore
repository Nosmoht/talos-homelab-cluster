talos/talosconfig
talos/generated/
talos/.secrets.dec.yaml
talos/.versions.stamp
/cluster.yaml
/talos/_out/
/kubernetes/bootstrap/argocd/_out/
# Transitional: keep ignored until all developer workstations have run `make migrate-cluster-yaml`.
.claude/environment.yaml
Plans/

# MCP config local overrides and pre-apply snapshots
.mcp.json.local
.mcp.json.bak-*

# validate-gitops scratch dir (rendered manifests, trivy report, conftest lists).
.work/

# Vendored OCI base (Phase C of rendered-manifests migration).
# Pulled by `make pull-base-oci` from ghcr.io/nosmoht/talos-platform-base
# at the tag pinned in .base-version. Never committed — every developer
# (and CI) re-pulls it deterministically; the tarball SHA is verified
# by cosign keyless OIDC.
vendor/base/

# Auto Claude data directory
.auto-claude/

# Auto Claude generated files
.auto-claude-security.json
.auto-claude-status
.claude_settings.json
.worktrees/
.security-key
logs/security/
