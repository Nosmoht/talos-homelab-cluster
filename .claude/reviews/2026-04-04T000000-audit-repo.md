---
generated_by: audit-repo
schema_version: 2
date: 2026-04-04
target: /Users/ntbc/workspace/Talos-Homelab
existing_claude_config: true
languages: [yaml, shell, makefile]
repo_type: Mixed
intervention_count: 8
p0_count: 2
p1_count: 4
p2_count: 2
summary:
  - error_class: Navigation
    gap: "No architecture entry-point map for 29 apps × 124 kustomization files"
    primitive: CLAUDE.md
    priority: P0
    token_impact: High
    signal_source: "—"
  - error_class: Navigation
    gap: "Vendor-generated large files (kubevirt/CDI operators, cilium bootstrap) not excluded from search scope"
    primitive: CLAUDE.md
    priority: P0
    token_impact: High
    signal_source: "—"
  - error_class: Repetition
    gap: "No end-to-end new application onboarding skill (namespace → overlay → Application CR → PNI labels → validate)"
    primitive: Skill
    priority: P1
    token_impact: Medium
    signal_source: "Repeated CLAUDE.md workflow"
  - error_class: Repetition
    gap: "No CI validation debug skill (reproduce gitops-validate.yml failures locally)"
    primitive: Skill
    priority: P1
    token_impact: Medium
    signal_source: "CI workflow complexity"
  - error_class: Convention
    gap: "No pre-write SOPS check; unencrypted secrets detectable only in CI"
    primitive: Hook
    priority: P1
    token_impact: Low
    signal_source: "—"
  - error_class: Navigation
    gap: ".auto-claude/worktrees/ not documented as excluded search path; inflates depth to 16"
    primitive: Rule
    priority: P1
    token_impact: Low
    signal_source: "—"
  - error_class: Repetition
    gap: "No LINSTOR/storage-SRE agent (own toolchain kubectl linstor + own evaluation criteria)"
    primitive: Agent
    priority: P2
    token_impact: Low
    signal_source: "IaC files"
  - error_class: Domain
    gap: "No glossary; PNI, AppProject, sync-wave, schematic, macvlan, DRBD, CCNP/CNP defined scattered across 10 rule files"
    primitive: CLAUDE.md
    priority: P2
    token_impact: Low
    signal_source: "—"
---

# Repository Audit Report

## Repository Profile

- **Target:** `/Users/ntbc/workspace/Talos-Homelab`
- **Languages:** YAML, Shell, Makefile
- **Frameworks:** ArgoCD, Kustomize, Helm, Talos Linux, Cilium, LINSTOR/Piraeus, KubeVirt, Kyverno
- **Existing Claude Config:** yes — 12 skills, 4 agents, 10 rules, hooks: no
- **Repository Type:** Mixed (IaC/GitOps source with extensive Claude Code primitives)

---

## Static Analysis Findings

### Toolchain

| Tool | Source File | Commands |
|------|-------------|----------|
| Make | `Makefile` | `argocd-install`, `argocd-bootstrap`, `argocd-password`, `argocd-oidc`, `grafana-dashboards-check`, `validate-gitops`, `validate-kyverno-policies` |
| Make | `talos/Makefile` | `gen-configs`, `gen-secrets`, `schematics`, `apply-all`, `dry-run-all`, `bootstrap`, `cilium-bootstrap`, `cilium-bootstrap-check`, `install-<node>`, `clean` |
| Shell scripts | `scripts/` | `discover_kustomize_targets.sh`, `render_kustomize_safe.sh`, `render-cilium-bootstrap.sh`, `run_conftest.sh`, `discover_argocd_apps.sh`, `verify_sops_files.sh` |
| GitHub Actions | `.github/workflows/gitops-validate.yml` | kustomize render, kubeconform, trivy, kyverno-cli, conftest, SOPS verification |

NOT FOUND: package.json, pyproject.toml, go.mod, Cargo.toml, Dockerfile, docker-compose.yml

CLAUDE.md lists required tools (`talosctl`, `kubectl`, `kubectl linstor`, `make`, `sops`, `yq`, `curl`, `jq`) and AGENTS.md has build targets. Toolchain is discoverable but no quick-reference command list in CLAUDE.md for the most common workflows.

### Ambiguity Metrics

| Metric | Value | Threshold | Status |
|--------|-------|-----------|--------|
| Max directory depth | 16 | >4 | WARN |
| Max files per directory | 42 (Plans/) | >30 | WARN |
| Naming collisions | 2 (base/overlay shared dir names, e.g. `infrastructure/`) | >5 | OK |
| Sprawl score | 1,344 | >100 | **P0** |

Note: Max depth of 16 is heavily inflated by `.auto-claude/worktrees/` shadow directories. Actual GitOps source depth is ~7-8. Plans/ contains 42 review artifacts and agent run outputs. 124 `kustomization.yaml` files across 29 infrastructure applications represent expected GitOps structure, not disorganization.

### Convention Enforcement

| Convention | Tier | Tool/Config | Gap |
|-----------|------|-------------|-----|
| Kubernetes manifests (schema, violations) | CI-enforced | `.github/workflows/gitops-validate.yml` (kubeconform, trivy) | No local pre-edit hook |
| SOPS encryption | CI-enforced | `scripts/verify_sops_files.sh` | No local pre-write hook |
| Kyverno PNI policies | CI-enforced | `.github/workflows/gitops-validate.yml` (kyverno-cli) | No local enforcement |
| Kubernetes recommended labels | AI-instructed | `CLAUDE.md` + `.claude/rules/manifest-quality.md` | covered |
| File naming (cnp-*, ccnp-*) | AI-instructed | `CLAUDE.md` + `.claude/rules/cilium-network-policy.md` | covered |
| Commit message format | AI-instructed | `CLAUDE.md` (Conventional Commits) | covered |
| YAML indentation | AI-instructed | `AGENTS.md` (2-space) | No deterministic linter |
| Pre-commit hooks | Undocumented | none (.pre-commit-config.yaml not found) | **gap** |

### Architecture

**Pattern:** ArgoCD App-of-Apps with Kustomize base/overlay + Talos cluster-as-code

**Evidence:**
- `kubernetes/bootstrap/` — one-time ArgoCD + Cilium bootstrap
- `kubernetes/overlays/homelab/infrastructure/` — 29 infrastructure components as ArgoCD Applications
- `kubernetes/overlays/homelab/projects/` — AppProject RBAC boundaries
- `talos/` — node configs (patches, secrets, factory schematics), `talos/versions.mk` pins all versions
- Sync-wave ordering: AppProjects(-1) → infrastructure(0) → apps(1)

**ADRs:** No formal `docs/adr/` directory. Architecture documented via rule files (`.claude/rules/kubernetes-gitops.md`, `.claude/rules/argocd-operations.md`) and CLAUDE.md.

**DI Framework:** None (declarative IaC replaces code-based DI).

### Domain Knowledge

| Type | Path | Summary |
|------|------|---------|
| Cluster environment | `cluster.yaml` | 8 nodes (3 CP, 3 worker, 1 GPU, 1 Pi), IPs, hardware specs |
| Software versions | `talos/versions.mk` | Talos v1.12.6, K8s v1.35.0, Cilium 1.19.2 |
| Platform Network Interface | `docs/platform-network-interface.md` | PNI capability catalog (13+ capabilities) |
| GitOps patterns | `.claude/rules/kubernetes-gitops.md` | App-of-Apps structure, sync-wave, multi-source Helm |
| Talos config | `.claude/rules/talos-config.md` | Patch layering, schematic flow, Makefile targets |
| Talos operations | `.claude/rules/talos-operations.md` | Node connectivity, upgrade gotchas, node recovery |
| ArgoCD operations | `.claude/rules/argocd-operations.md` | Safe change sequence, drift handling |
| Upgrade history | `docs/talos-upgrade-plan-*.md`, `docs/talos-upgrade-report-*.md` | Executed upgrade plans with verification |
| Postmortems | `docs/postmortem-gateway-403-hairpin.md` | Gateway hairpin issue analysis |
| Day-2 operations | `docs/day2-operations.md` | Operational runbooks |
| Hardware analysis | `docs/hardware-analysis-node-*.md` | Per-node specs |

**Gaps:**
- No `GLOSSARY.md` — domain terms defined inline across multiple files
- No formal ADR directory — decisions embedded in rule files and CLAUDE.md
- No OpenAPI/gRPC/GraphQL schemas (not applicable — infrastructure repo)

---

## Token Efficiency Findings

### File Size Distribution

| File | Lines | Classification | Est. Tokens |
|------|-------|---------------|-------------|
| `kubernetes/overlays/homelab/infrastructure/kubevirt/resources/kubevirt-operator.yaml` | 7,669 | **severe** | ~38,345 |
| `kubernetes/overlays/homelab/infrastructure/kubevirt-cdi/resources/cdi-operator.yaml` | 5,486 | **severe** | ~27,430 |
| `kubernetes/bootstrap/cilium/cilium.yaml` | 2,339 | **severe** | ~11,695 |
| `Plans/network-infrastructure-analysis.md` | 1,352 | critical | ~8,112 |
| `.claude/reviews/2026-03-24T134500-review-claude-config.md` | 1,036 | critical | ~6,216 |
| `docs/kernel-tuning.md` | 770 | token sink | ~4,620 |
| `kubernetes/overlays/homelab/infrastructure/tetragon/resources/grafana-dashboard.yaml` | 754 | token sink | ~3,770 |
| `.claude/reviews/2026-03-24T170000-review-claude-config.md` | 743 | token sink | ~4,458 |

**Key Finding:** The two vendor-generated KubeVirt operator YAML files together constitute ~65K tokens and are never edited directly. Any broad file search will pull these in. `cilium.yaml` (2,339 lines) is generated by `make cilium-bootstrap` — also not hand-edited.

### Navigation Sprawl Score

```
Score: 1,344
Classification: P0 — architecture map with entry points needed
Breakdown: depth 16 × files/dir 42 × collisions 2

Caveat: depth 16 is .auto-claude/worktrees/ inflated.
Real source depth ~7-8 levels. Effective sprawl ~672 (still P0).
```

### Build Error Verbosity

| Toolchain | Verbosity | Token Cost |
|-----------|-----------|------------|
| Makefile (`validate-gitops`) | Medium | ~5-8K per run (spawns 4 scripts + kubeconform + trivy) |
| `talos/Makefile` | Medium | ~500-800 tokens per invocation |
| GitHub Actions YAML | Low | Reference only |
| `.mk` version files | Low | ~40-80 tokens |

### Monorepo Isolation

```
Packages: 29 distinct K8s infrastructure applications
Cross-imports: N/A (GitOps IaC — Kustomize patches define dependencies, not code imports)
Assessment: Strong isolation via ArgoCD Application CRs and AppProject RBAC boundaries.
            Kustomize base/overlay cleanly separates upstream defaults from homelab-specific values.
            Bootstrap/overlays split separates one-time setup from day-2 GitOps state.
```

### Context Burn Rate

| Task Type | Est. Tokens |
|-----------|-------------|
| Simple edit (known Kustomize path) | 3–5K |
| Exploration + edit (new app integration) | 15–25K |
| Multi-file refactor (e.g., network policy overhaul) | 40–70K |

**Burn risk:** Exploring without knowing which kustomization file to edit first may accidentally load the 38K-token kubevirt-operator.yaml. Broad YAML searches are expensive.

---

## Intervention Matrix

| # | Error Class | Gap | Primitive | Priority | Token Impact | Signal Source | Evidence |
|---|-------------|-----|-----------|----------|-------------|--------------|----------|
| 1 | Navigation | No app→overlay entry-point map for 29 apps × 124 kustomization files | CLAUDE.md | P0 | High | — | 124 `kustomization.yaml` files, `kubernetes/overlays/homelab/infrastructure/` has 29+ subdirectories, sprawl score 1,344 |
| 2 | Navigation | Vendor-generated large files (~65K tokens) have no exclusion hint | CLAUDE.md | P0 | High | — | `kubevirt-operator.yaml` (7,669 lines), `cdi-operator.yaml` (5,486 lines), `cilium.yaml` (2,339 lines) |
| 3 | Repetition | No end-to-end new application onboarding skill | Skill | P1 | Medium | Repeated CLAUDE.md workflow | PNI Mandatory Workflow in CLAUDE.md, 29 existing overlays as examples, `kubernetes-gitops.md` steps |
| 4 | Repetition | No CI validation debug skill (reproduce gitops-validate.yml failures locally) | Skill | P1 | Medium | CI workflow complexity | `.github/workflows/gitops-validate.yml` has 5+ tools; scripts require different invocations |
| 5 | Convention | No pre-write SOPS check; unencrypted secrets detectable only in CI | Hook | P1 | Low | — | `scripts/verify_sops_files.sh` present in CI; no `.pre-commit-config.yaml`; no Claude hook |
| 6 | Navigation | `.auto-claude/worktrees/` not documented as excluded search path | Rule | P1 | Low | — | Token Analyzer found depth 16 due to worktree paths; no rule file excludes these |
| 7 | Repetition | No LINSTOR/storage-SRE agent (own toolchain + own evaluation criteria) | Agent | P2 | Low | IaC files | `kubectl linstor` in Required Tools; piraeus-datastore namespace; NFD storage label |
| 8 | Domain | No glossary; key terms scattered across 10 rule files | CLAUDE.md | P2 | Low | — | No `GLOSSARY.md` found; terms defined across CLAUDE.md, argocd-operations.md, kubernetes-gitops.md, etc. |

---

## Recommendations

### P0 — Immediate (CLAUDE.md Navigation)

**#1 — App entry-point map**

Add a "Quick Navigation" section to CLAUDE.md (below Cluster Overview):

```markdown
## Quick Navigation

### Kubernetes Overlay Paths
- App overlays: `kubernetes/overlays/homelab/infrastructure/<app-name>/`
  - Kustomization: `kustomization.yaml`
  - Helm values: `values.yaml` (or `values-<component>.yaml`)
  - Resources: `resources/*.yaml` (CRs, CNPs, secrets)
- Base definitions: `kubernetes/base/infrastructure/<app-name>/` (shared, upstream)
- Application CRs: `kubernetes/overlays/homelab/apps/`
- AppProject CRs: `kubernetes/overlays/homelab/projects/`
- Bootstrap (one-time, do not edit): `kubernetes/bootstrap/`

To find which overlay to edit for app X: `kubernetes/overlays/homelab/infrastructure/X/`
```

**#2 — Vendor file exclusion hints**

Add to CLAUDE.md under Quick Navigation:

```markdown
### Exclude from searches
- `kubernetes/overlays/homelab/infrastructure/kubevirt/resources/kubevirt-operator.yaml` — vendor-generated (7,669 lines), do not edit
- `kubernetes/overlays/homelab/infrastructure/kubevirt-cdi/resources/cdi-operator.yaml` — vendor-generated (5,486 lines), do not edit
- `kubernetes/bootstrap/cilium/cilium.yaml` — generated by `make cilium-bootstrap` (2,339 lines), do not hand-edit
- `.auto-claude/worktrees/` — Claude Code worktree snapshots, not source of truth
```

---

### P1 — Short-term (Skill + Hook + Rule)

**#3 — New application onboarding skill**

Create `.claude/skills/new-app-onboarding/SKILL.md`. Steps:
1. Create namespace manifest with PNI labels (`platform.io/network-interface-version: v1`, `network-profile`, `consume.<capability>`)
2. Create `kubernetes/overlays/homelab/infrastructure/<app-name>/` with kustomization.yaml, values.yaml, Application CR
3. Set Kubernetes recommended labels on all resources
4. Run `make validate-gitops` locally
5. Verify kyverno-cli passes PNI policy checks
6. Commit and push (ArgoCD syncs)

Use existing overlays (e.g., `cert-manager/`, `loki/`) as skeleton examples.

**#4 — CI validation debug skill**

Create `.claude/skills/gitops-ci-debug/SKILL.md`. Steps:
1. Identify which CI step failed (kustomize render, kubeconform, trivy, conftest, kyverno-cli, SOPS)
2. Reproduce locally: `scripts/render_kustomize_safe.sh`, `scripts/run_conftest.sh`, individual kubeconform invocations
3. For kyverno-cli failures: `kyverno-cli apply --policy ...`
4. For trivy failures: `trivy config kubernetes/overlays/...`
5. Fix manifest, re-verify, commit

**#5 — SOPS pre-write hook**

Add a PreToolUse Write hook in `.claude/settings.json`:

```json
{
  "hooks": {
    "PreToolUse": [{
      "matcher": "Write|Edit",
      "hooks": [{
        "type": "command",
        "command": "scripts/verify_sops_files.sh --check-file \"$CLAUDE_TOOL_INPUT_FILE_PATH\""
      }]
    }]
  }
}
```

Requires adapting `verify_sops_files.sh` to accept a single file path argument.

**#6 — Worktree exclusion rule**

Create `.claude/rules/search-scope.md`:

```markdown
## Search Scope Exclusions
- Exclude `.auto-claude/worktrees/` from all Glob and Grep searches — worktree snapshots, not canonical source
- Exclude vendor-generated resources from broad YAML searches (see CLAUDE.md exclude list)
- Prefer searching `kubernetes/overlays/homelab/` over `kubernetes/` root to avoid base/overlay ambiguity
```

---

### P2 — Medium-term (Agent + Domain)

**#7 — LINSTOR/storage-SRE agent**

Create `.claude/agents/linstor-storage-sre.md`. Scope:
- Own toolchain: `kubectl linstor`, piraeus-datastore namespace operations
- Own evaluation criteria: DRBD replication health, storage pool capacity, NVMe node labels (`feature.node.kubernetes.io/storage-nvme.present=true`)
- Use cases: creating StorageClasses, debugging PVC binding failures, adding storage capacity, DRBD split-brain recovery

**#8 — Glossary pointer**

Add to CLAUDE.md as a short "Key Terms" section:

```markdown
## Key Terms
- **PNI (Platform Network Interface)**: Kyverno-enforced namespace contract for platform service access. See `docs/platform-network-interface.md`.
- **AppProject**: ArgoCD RBAC boundary scoping which repos/namespaces an Application can deploy to.
- **Sync-wave**: ArgoCD annotation controlling deploy order: `-1` (AppProjects) → `0` (infrastructure) → `1` (apps).
- **Schematic**: Talos Image Factory spec embedding system extensions into installer images. See `talos/.schematic-ids.mk`.
- **CCNP/CNP**: CiliumClusterwideNetworkPolicy / CiliumNetworkPolicy. Named `ccnp-*.yaml` / `cnp-*.yaml`.
- **macvlan**: Virtual NIC on physical interface. Used for `ingress-front` stable MAC assignment.
- **DRBD**: Distributed Replicated Block Device — LINSTOR replication layer for persistent storage.
```

---

## Next Steps

- Create or update CLAUDE.md with P0 items first (app entry-point map + vendor file exclusions)
- Use `/scaffold-skill new-app-onboarding` and `/scaffold-skill gitops-ci-debug` for skill candidates
- Run `/suggest-skills /Users/ntbc/workspace/Talos-Homelab` for open-reasoning skill discovery beyond this catalog
- Re-run `/audit-repo /Users/ntbc/workspace/Talos-Homelab` after changes to verify coverage
- Run `/review-claude-config /Users/ntbc/workspace/Talos-Homelab` to evaluate quality after creating primitives
