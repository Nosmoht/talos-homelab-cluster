---
generated_by: suggest-skills
schema_version: 1
date: 2026-03-24
target: /Users/ntbc/workspace/Talos-Homelab
repo_type: Mixed
existing_skills: 10
suggestions:
  - name: storage-health-preflight
    priority: High
    score: 9
    signal_sources: ["Documentation", "Existing Skills", "Tech Stack"]
    criteria_passed: 4
  - name: pni-onboard-namespace
    priority: High
    score: 8
    signal_sources: ["Documentation", "Quality & Config"]
    criteria_passed: 4
  - name: version-compatibility-check
    priority: High
    score: 7
    signal_sources: ["CI/CD", "Existing Skills", "Documentation"]
    criteria_passed: 4
  - name: ci-validation-triage
    priority: High
    score: 7
    signal_sources: ["CI/CD", "Quality & Config"]
    criteria_passed: 3
  - name: sops-secret-management
    priority: High
    score: 7
    signal_sources: ["Git Conventions", "CI/CD", "Tech Stack"]
    criteria_passed: 3
  - name: monitoring-triage
    priority: Medium
    score: 6
    signal_sources: ["Documentation", "Tech Stack"]
    criteria_passed: 3
  - name: argocd-bootstrap
    priority: Medium
    score: 6
    signal_sources: ["Documentation", "CI/CD"]
    criteria_passed: 3
  - name: reference-freshness-check
    priority: Medium
    score: 5
    signal_sources: ["Existing Skills", "Tech Stack"]
    criteria_passed: 3
---

# Skill Suggestions Report

## Repository Overview

- **Target:** /Users/ntbc/workspace/Talos-Homelab
- **Repository Type:** Mixed (IaC infrastructure + Claude Code skills/agents)
- **Tech Stack:** Talos v1.12.6, Kubernetes v1.35.0, Cilium v1.19.2, ArgoCD (Helm 9.4.5), LINSTOR/Piraeus CSI with DRBD, Gateway API, SOPS/AGE, Prometheus/Grafana/Loki monitoring stack
- **Existing Skills:** 10 (gitops-health-triage, talos-node-maintenance, cilium-policy-debug, plan-talos-upgrade, execute-talos-upgrade, plan-cilium-upgrade, execute-cilium-upgrade, optimize-node-kernel, analyze-node-hardware, update-schematics)
- **Existing Agents:** 3 (gitops-operator, talos-sre, platform-reliability-reviewer)
- **Suggestions Generated:** 8

---

## Suggestions

### 1. storage-health-preflight (Priority: High, Score: 9/9)

**Signal Sources:**
- CLAUDE.md documents DRBD D-state deadlocks requiring physical power cycles (Category A)
- `talos-node-maintenance` includes narrow single-node LINSTOR check but no cluster-wide assessment (Category B)
- `docs/day2-operations.md` documents DRBD drain requirement before reboot operations (Category A)
- 7 nodes with DRBD replication across NVMe, complex replica topology (Category C)

**Extraction Criteria:** 4/4 passed — Recurrence (storage checks in 4+ contexts), Verification (6 clear steps), Non-obviousness (DRBD topology + D-state failure mode + drain interaction), Generalizability (any maintenance window, any node)

**Rationale:** The most catastrophic failure mode in this cluster (DRBD D-state requiring physical power cycle) is caused by insufficient pre-maintenance storage checks. The existing `talos-node-maintenance` skill only does a narrow single-node volume check. A cluster-wide storage health assessment prevents data unavailability during maintenance windows.

**Web Evidence:** LINBIT documentation confirms LINSTOR exposes resource health and replica states via CLI. Kubernetes drain documentation emphasizes pre-drain replica verification for storage-backed workloads.

**Skeleton SKILL.md:**
```yaml
---
name: storage-health-preflight
description: "Assess LINSTOR/DRBD storage health across the cluster. Check replica counts, DRBD connection states, storage pool capacity, and node drain safety. Use before maintenance windows, upgrades, or when investigating storage issues."
argument-hint: "[node-name|all]"
allowed-tools: Bash, Read, Grep, Glob
---

# Storage Health Preflight

You are a storage reliability engineer assessing LINSTOR/DRBD health in a Talos
Kubernetes cluster. Produce a clear safety assessment: which nodes are safe to
drain, which volumes are at risk, and what must be resolved before maintenance.

## Key Steps
1. Verify LINSTOR controller is running via `kubectl linstor node list`.
2. List all DRBD resources; identify under-replicated, degraded, or faulty resources.
3. Check DRBD connection states for SyncTarget or StandAlone nodes.
4. Check storage pool capacity and thin-pool usage.
5. For a specific node, assess drain safety: verify every volume has a healthy replica elsewhere.
6. Produce health report with SAFE/UNSAFE/WARNING verdicts per node.

## Notes
This is a starting-point skeleton, not a production-ready skill.
```

**Recommended Reference Files:**
- references/linstor-health-checks.md — LINSTOR CLI commands for resource state, replica topology, storage pool capacity, DRBD connection state interpretation
- references/drbd-failure-modes.md — D-state deadlock conditions, power cycle recovery, pre-drain checklist

---

### 2. pni-onboard-namespace (Priority: High, Score: 8/9)

**Signal Sources:**
- `docs/platform-network-interface.md` defines 5-step onboarding workflow with validation checklist (Category A)
- CLAUDE.md has 7 PNI-related rules including 5 contract rules (Category A)
- 11 capabilities in the PNI capability catalog, each requiring correct label combinations (Category A)
- Kyverno policies enforce PNI contract compliance: reserved labels, network profile (Category F)

**Extraction Criteria:** 4/4 passed — Recurrence (every new namespace must onboard), Verification (6 clear steps), Non-obviousness (PNI contract + capability catalog + Kyverno enforcement + DNAT gotcha), Generalizability (any namespace, any capability combination)

**Rationale:** PNI is a custom platform abstraction with 11 capabilities, 3 profiles, reserved labels, and Kyverno enforcement. Getting any step wrong results in blocked traffic or admission denials. This skill bridges the gap between PNI documentation and actual namespace creation.

**Web Evidence:** Kubernetes namespace onboarding automation with labels, quotas, and policy scaffolding is a recognized platform engineering pattern. Kyverno supports automated label validation during onboarding.

**Skeleton SKILL.md:**
```yaml
---
name: pni-onboard-namespace
description: "Scaffold a new PNI-compliant namespace with correct labels, capability opt-ins, and validation. Generates namespace YAML, validates against Kyverno policies, and verifies kustomize rendering. Use when adding a new application namespace to the cluster."
argument-hint: "<namespace-name>"
disable-model-invocation: true
allowed-tools: Bash, Read, Grep, Glob, Write, Edit
---

# PNI Namespace Onboarding

You are a platform engineer onboarding a new consumer namespace into the PNI
framework. Scaffold correct namespace metadata, validate policy compliance, and
ensure the namespace is ready for deployment.

## Key Steps
1. Read `docs/platform-network-interface.md` for current capability catalog and contract version.
2. Ask the user for namespace name, network profile (restricted/managed), and required capabilities.
3. Generate namespace YAML with PNI v1 labels, profile, and capability opt-ins.
4. Validate against Kyverno policies (reserved label check, required label check).
5. Add namespace to appropriate kustomization.yaml and render with kustomize.
6. Present generated files for user review before writing.

## Notes
This is a starting-point skeleton, not a production-ready skill.
```

**Recommended Reference Files:**
- references/pni-contract-v1.md — Current PNI contract version, required labels, capability catalog snapshot, reserved label list
- references/namespace-scaffold-template.md — Template namespace YAML with PNI label placeholders

---

### 3. version-compatibility-check (Priority: High, Score: 7/9)

**Signal Sources:**
- `talos/versions.mk` centralizes TALOS_VERSION, KUBERNETES_VERSION, CILIUM_VERSION (Category D)
- Existing plan-talos-upgrade and plan-cilium-upgrade operate independently without cross-validation (Category B)
- Cilium deployed via Talos extraManifests creates tight coupling (Category A)
- Upgrade docs show sequential version bumps across components (Category E)

**Extraction Criteria:** 4/4 passed — Recurrence (every upgrade cycle), Verification (6 clear steps), Non-obviousness (three-way compatibility + extraManifests coupling + upgrade ordering), Generalizability (any version combination)

**Rationale:** Discovering an incompatible version combination after starting a rolling upgrade is far more costly than catching it during planning. This fills the gap between independent plan-talos-upgrade and plan-cilium-upgrade skills.

**Web Evidence:** Cilium publishes a Kubernetes compatibility matrix. Talos documentation includes a version support matrix. GitHub discussions (siderolabs/talos) show users hitting "unsupported upgrade path" errors from version mismatches.

**Skeleton SKILL.md:**
```yaml
---
name: version-compatibility-check
description: "Validate that a proposed combination of Talos, Kubernetes, and Cilium versions is compatible before starting an upgrade. Checks official support matrices and reports upgrade ordering constraints. Use before plan-talos-upgrade or plan-cilium-upgrade."
argument-hint: "[talos=vX.Y.Z] [k8s=vX.Y.Z] [cilium=vX.Y.Z]"
allowed-tools: Bash, Read, Grep, Glob, WebSearch, WebFetch
---

# Version Compatibility Check

You are an upgrade planning engineer validating that a proposed version combination
for this Talos/Kubernetes/Cilium cluster is safe. Consult official support matrices
and report compatibility status with recommended upgrade ordering.

## Key Steps
1. Read current versions from `talos/versions.mk`.
2. Parse proposed versions from user arguments (unspecified default to current).
3. Fetch the Talos support matrix to verify the Talos-to-K8s version pairing.
4. Fetch the Cilium compatibility matrix to verify the Cilium-to-K8s version pairing.
5. Check for known constraints (extraManifests coupling, single-step upgrade limits).
6. Report compatibility verdict and recommended upgrade order.

## Notes
This is a starting-point skeleton, not a production-ready skill.
```

**Recommended Reference Files:**
- references/version-matrix-sources.md — URLs for Talos support matrix, Cilium compatibility matrix, known cross-component constraints for this cluster's deployment pattern

---

### 4. ci-validation-triage (Priority: High, Score: 7/9)

**Signal Sources:**
- `gitops-validate.yml`: 393 lines with 15+ validation phases (Category D)
- No existing skill covers CI failure diagnosis (Category B)
- 7 distinct validation tools in pipeline: kustomize, conftest, kubeconform, trivy, kyverno, sops, ksops (Category F)
- 6 shell scripts orchestrating validation steps (Category D)

**Extraction Criteria:** 3/4 passed — Recurrence (CI failures recur across PRs), Verification (7 steps: identify failed phase, read logs, classify error, suggest fix), Non-obviousness (15 phases with different tools, error messages vary by tool), Generalizability (any CI failure)

**Rationale:** A 393-line CI workflow with 15+ validation phases and 7 different tools creates a complex failure space. When CI fails, developers need to identify which phase, understand the tool-specific error, and know the correct fix. This skill reduces mean-time-to-fix for CI failures.

**Web Evidence:** Complex CI pipeline triage is a recognized DevOps workflow. GitHub Actions debugging documentation covers job output parsing and failure classification.

**Skeleton SKILL.md:**
```yaml
---
name: ci-validation-triage
description: "Diagnose gitops-validate CI failures. Identify which validation phase failed (kustomize, conftest, kubeconform, trivy, kyverno, sops), parse the error, and suggest the fix. Use when a PR CI check fails."
argument-hint: "[pr-number|workflow-run-url]"
allowed-tools: Bash, Read, Grep, Glob
---

# CI Validation Triage

You are a CI debugging specialist for the gitops-validate workflow. Identify the
failed phase, parse tool-specific error messages, and provide actionable fixes.

## Key Steps
1. Fetch CI run logs via `gh run view` or `gh pr checks`.
2. Identify which validation phase failed (kustomize, conftest, kubeconform, trivy, kyverno, sops).
3. Parse the tool-specific error message.
4. Map the error to known patterns (e.g., schema mismatch, policy violation, security finding).
5. Identify the source file causing the failure.
6. Suggest a concrete fix with the specific file and change needed.
7. If applicable, provide local reproduction command.

## Notes
This is a starting-point skeleton, not a production-ready skill.
```

**Recommended Reference Files:**
- references/ci-phase-map.md — Map of each CI phase to its tool, expected inputs, common errors, and fix patterns

---

### 5. sops-secret-management (Priority: High, Score: 7/9)

**Signal Sources:**
- `.sops.yaml` defines two path-based encryption rules with AGE backend (Category E)
- CI verifies SOPS metadata without decryption via `verify_sops_files.sh` (Category D)
- KSOPS plugin integrates SOPS with kustomize for ArgoCD (Category C)
- 2 components use SOPS (cert-manager, dex), plus Talos secrets (Category A)
- No existing skill covers secret lifecycle (Category B)

**Extraction Criteria:** 3/4 passed — Recurrence (any secret change requires SOPS workflow), Verification (6 steps: identify target, decrypt, edit, re-encrypt, validate, commit), Non-obviousness (two path patterns, AGE key management, KSOPS integration, CI validation interaction), Generalizability (any encrypted secret)

**Rationale:** SOPS secret management in this repo spans multiple patterns (Kubernetes secrets via KSOPS, Talos secrets via AGE), involves a CI validation gate, and mistakes can expose secrets in git. A skill ensures the correct encryption path rules are applied and CI will pass.

**Web Evidence:** SOPS is the standard GitOps secret management tool. Mozilla's SOPS documentation emphasizes the importance of correct `.sops.yaml` path rules to prevent plaintext secret commits.

**Skeleton SKILL.md:**
```yaml
---
name: sops-secret-management
description: "Manage SOPS-encrypted secrets in the cluster. Create, edit, rotate, or verify encrypted secrets with correct AGE keys and path rules. Validates SOPS metadata and ensures CI compatibility. Use when adding or modifying secrets."
argument-hint: "<secret-path>"
disable-model-invocation: true
allowed-tools: Bash, Read, Grep, Glob, Write, Edit
---

# SOPS Secret Management

You are a secrets engineer managing SOPS-encrypted files in a GitOps workflow.
Ensure correct encryption, path rule compliance, and CI validation compatibility.

## Key Steps
1. Read `.sops.yaml` to identify encryption rules and AGE recipients.
2. Determine target path and which rule applies (talos/ vs kubernetes/).
3. If editing: decrypt with `sops -d`, apply changes, re-encrypt with `sops -e`.
4. If creating: scaffold the secret YAML, encrypt with `sops -e`.
5. Verify SOPS metadata is present (mac, encrypted_regex fields).
6. Validate with `scripts/verify_sops_files.sh` to ensure CI will pass.

## Notes
This is a starting-point skeleton, not a production-ready skill.
```

**Recommended Reference Files:**
- references/sops-path-rules.md — Current `.sops.yaml` path patterns, AGE recipient mapping, KSOPS integration notes

---

### 6. monitoring-triage (Priority: Medium, Score: 6/9)

**Signal Sources:**
- CLAUDE.md Monitoring section: 7 gotchas including scheduler reachability, bind-address issues, dashboard filtering (Category A)
- Monitoring stack: Prometheus (kube-prometheus-stack), Grafana, Alertmanager, Loki, AllOy (Category C)
- No existing skill covers monitoring/observability diagnosis (Category B)

**Extraction Criteria:** 3/4 passed — Recurrence (monitoring issues appear across scheduler, kubelet, exporters), Verification (6 steps: check targets, identify scrape failures, diagnose CNP blocking, fix), Non-obviousness (Cilium CNP interactions with scrape endpoints, bind-address configuration, cross-namespace scraping), Generalizability (any Prometheus/Grafana issue)

**Rationale:** The 7 documented monitoring gotchas in CLAUDE.md indicate recurring operational friction. The interaction between Cilium network policies and Prometheus scraping is particularly non-obvious and unique to this cluster's CNI choice.

**Web Evidence:** Prometheus in Cilium environments is a documented challenge — Cilium's docs have a dedicated page on monitoring policy compatibility.

**Skeleton SKILL.md:**
```yaml
---
name: monitoring-triage
description: "Diagnose Prometheus scrape failures, Grafana dashboard issues, and Alertmanager routing problems. Handles Cilium CNP interactions with monitoring endpoints. Use when metrics are missing, dashboards show no data, or alerts are not firing."
argument-hint: "[target-name|dashboard|alert-name]"
allowed-tools: Bash, Read, Grep, Glob
---

# Monitoring Triage

You are a monitoring reliability engineer diagnosing observability issues in a
Talos/Cilium cluster with Prometheus, Grafana, Loki, and Alertmanager.

## Key Steps
1. Identify the symptom (missing metrics, empty dashboard, silent alert).
2. Check Prometheus targets for scrape failures via port-forward or kubectl.
3. If scrape fails: check CiliumNetworkPolicy for missing TCP port allowances.
4. Check bind-address configuration on scheduler/controller-manager endpoints.
5. For dashboard issues: verify datasource configuration and Grafana sidecar import.
6. Provide specific fix (CNP rule, bind-address override, dashboard JSON correction).

## Notes
This is a starting-point skeleton, not a production-ready skill.
```

**Recommended Reference Files:**
- references/monitoring-gotchas.md — Known scrape target issues, bind-address overrides, CNP port requirements for monitoring namespace

---

### 7. argocd-bootstrap (Priority: Medium, Score: 6/9)

**Signal Sources:**
- README.md documents 3-step bootstrap flow: Talos provisioning → ArgoCD bootstrap → reconciliation (Category A)
- Root Makefile targets: argocd-install, argocd-bootstrap, argocd-password, argocd-oidc (Category D)
- Sync-wave ordering (-1 projects, 0 infrastructure, 1 apps) documented in CLAUDE.md and kubernetes-gitops.md rules (Category A)
- No existing skill covers initial cluster bootstrap or disaster recovery re-bootstrap (Category B)

**Extraction Criteria:** 3/4 passed — Recurrence (bootstrap needed for initial setup + disaster recovery), Verification (7 steps), Non-obviousness (sync-wave ordering, SOPS key injection, Cilium extraManifests prerequisite), Generalizability (FAIL — specific to this cluster's bootstrap sequence)

**Rationale:** While rare (initial setup or disaster recovery), bootstrap is the highest-stakes operation with the most ordering dependencies. The documented 3-step flow has multiple sub-steps and gotchas that a skill would capture.

**Web Evidence:** ArgoCD bootstrap automation is a recognized GitOps pattern. The ArgoCD docs describe app-of-apps bootstrapping as a recommended approach.

**Skeleton SKILL.md:**
```yaml
---
name: argocd-bootstrap
description: "Orchestrate initial cluster bootstrap or disaster recovery re-bootstrap. Runs the ArgoCD app-of-apps bootstrap sequence with sync-wave ordering, SOPS key injection, and post-bootstrap health verification. Use for fresh cluster setup or disaster recovery."
disable-model-invocation: true
allowed-tools: Bash, Read, Grep, Glob
---

# ArgoCD Bootstrap

You are a platform engineer bootstrapping an ArgoCD-managed cluster from scratch.
Follow the documented bootstrap sequence with strict ordering.

## Key Steps
1. Verify Talos cluster is healthy and Cilium is running.
2. Run `make argocd-install` (includes SOPS AGE key secret creation).
3. Run `make argocd-bootstrap` (applies root project + application).
4. Monitor sync-wave ordering: wave -1 (projects) → wave 0 (infrastructure) → wave 1 (apps).
5. Configure OIDC with `make argocd-oidc`.
6. Verify all applications reach Healthy/Synced state.
7. Run post-bootstrap health checks (LINSTOR, Cilium, monitoring).

## Notes
This is a starting-point skeleton, not a production-ready skill.
```

**Recommended Reference Files:**
- references/bootstrap-sequence.md — Ordered bootstrap steps, sync-wave dependencies, common failure points and recovery

---

### 8. reference-freshness-check (Priority: Medium, Score: 5/9)

**Signal Sources:**
- 10 skills with `references/` directories containing version-specific content (Category B)
- Talos v1.12.6, Cilium v1.19.2 — references may contain outdated version-specific guidance (Category C)
- No mechanism to detect stale references when components are upgraded (Category B)

**Extraction Criteria:** 3/4 passed — Recurrence (stale references accumulate with each version bump), Verification (5 steps), Non-obviousness (version mentions embedded in prose, not just filenames), Generalizability (any skill repository with versioned references)

**Rationale:** When Talos or Cilium is upgraded, skill reference files may contain outdated version-specific CLI flags, API endpoints, or behavioral notes. A freshness check after version bumps ensures references stay accurate.

**Skeleton SKILL.md:**
```yaml
---
name: reference-freshness-check
description: "Scan skill reference files for version-specific content that may be stale after component upgrades. Compares version mentions in references against talos/versions.mk and flags mismatches. Use after upgrading Talos, Kubernetes, or Cilium."
allowed-tools: Read, Grep, Glob
---

# Reference Freshness Check

You are a maintenance engineer checking skill reference files for version drift
after component upgrades.

## Key Steps
1. Read current versions from `talos/versions.mk`.
2. Scan all `references/*.md` files across skills for version mentions.
3. Compare mentioned versions against current deployed versions.
4. Flag files with outdated version references.
5. Report stale references with specific lines and suggested updates.

## Notes
This is a starting-point skeleton, not a production-ready skill.
```

**Recommended Reference Files:**
- references/version-patterns.md — Regex patterns for detecting version mentions in reference files

---

## Signal Summary

| Discovery Method | Suggestions |
|-----------------|-------------|
| Layer 1: Table matches | 5 |
| Layer 2: Open reasoning | 3 |
| **Total (after dedup)** | **8** |

## Integration Notes

- **Suggestions that complement existing skills:**
  - `storage-health-preflight` feeds into `talos-node-maintenance` (run storage preflight before node maintenance)
  - `version-compatibility-check` feeds into `plan-talos-upgrade` and `plan-cilium-upgrade` (run compatibility check before planning)
  - `ci-validation-triage` complements `platform-reliability-reviewer` agent (reviewer catches issues pre-commit, triage diagnoses CI failures post-push)
  - `sops-secret-management` intersects with the SOPS verification in CI (`verify_sops_files.sh`)
  - `reference-freshness-check` should run after `execute-talos-upgrade` or `execute-cilium-upgrade`

- **Suggested implementation order:**
  1. `storage-health-preflight` — highest impact, prevents catastrophic failure mode
  2. `version-compatibility-check` — safety net before any upgrade, complements existing plan skills
  3. `ci-validation-triage` — immediate daily utility for PR workflow
  4. `pni-onboard-namespace` — enables self-service namespace creation
  5. `sops-secret-management` — reduces secret management friction
  6. `monitoring-triage` — addresses documented recurring pain points
  7. `argocd-bootstrap` — rare but high-stakes, good documentation value
  8. `reference-freshness-check` — maintenance hygiene, lowest urgency
